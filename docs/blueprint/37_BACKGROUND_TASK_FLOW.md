# 37 — BACKGROUND

Laptop acik -> Docker calisiyor -> worker/scheduler daemon thread -> periyodik kontrol
Restart: worker task'lari sifirlanir, scheduler state'den yuklenir
Eksik: Worker task persistence, crash recovery, notification
